apiVersion: v1
kind: Service

metadata:
  name: inventario-service

spec:
  selector:
    app: inventario-app

    # Inicialmente todo el tráfico se dirige a Blue.
    slot: blue

  ports:
    - name: http
      port: 80
      targetPort: http

  type: NodePort